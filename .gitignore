# Dependencies
node_modules

# Nuxt build artifacts
.nuxt
.nitro
.cache
.output
dist

# Nuxt / Nitro data
.data

# Logs
*.log
logs

# Env
.env
.env.*
!.env.example

# Test / coverage
coverage
test-results
playwright-report
.playwright

# Editor / OS
.DS_Store
# Personal editor prefs stay local; shared toolchain config is tracked —
# settings.json pins Prettier as the formatter to match lint-staged.
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

# Vercel
.vercel

# Internal-only skill — distributed out-of-band, see .claude/CLAUDE.md
# No trailing slash: that form would not match if this becomes a symlink.
.claude/skills/product-spec
